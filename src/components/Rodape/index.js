import styles from './Rodape.module.css';

const Rodape = () => {
    return(
        <footer className={styles.footer}>
            <h2>Desenvolvido por Gabrielly</h2>
        </footer>
    )
}

export default Rodape;